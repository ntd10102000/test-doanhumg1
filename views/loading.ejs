<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>ProgressBar.js - Minimal Example</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        .progress {
            height: 300px;
        }
        
        .progress>svg {
            height: 100%;
            display: block;
        }
    </style>
</head>

<body>
    <div class="progress" id="progress"></div>
    <input type="hidden" class="s" id="geo" value="<%= t %>">
    <p id="p"></p>
    <a href="/manage" id="ok"></a>
    <!-- These are probably out dated so you might want to use newest versions -->
    <script src="https://cdn.rawgit.com/kimmobrunfeldt/progressbar.js/0.5.6/dist/progressbar.js"></script>

    <script>
        var time = parseInt(document.getElementById("geo").value);
        console.log(time);
        window.onload = function onLoad() {
            var circle = new ProgressBar.Circle('#progress', {
                color: '#FCB03C',
                duration: time,
                easing: 'easeInOut'
            });


            circle.animate(1);
        };

        setTimeout(() => {
            document.getElementById('p').innerHTML = 'Import thành công';
            document.getElementById('ok').innerHTML = 'Ok';
        }, time + 1000);
    </script>
</body>

</html>